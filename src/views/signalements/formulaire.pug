extends ../layout

block content
  h1 Nouveau signalement

  form(method="post", action="/signalements")
    input(type="text", name="titre", placeholder="Titre", required)
    textarea(name="description", placeholder="Description")
    input(type="url", name="photo", placeholder="URL de la photo")
    input(type="text", name="categorie", placeholder="Catégorie")
    input(type="text", name="longitude", placeholder="Longitude", required)
    input(type="text", name="latitude", placeholder="Latitude", required)
    select(name="statut")
      option(value="en attente") En attente
      option(value="en cours") En cours
      option(value="résolu") Résolu
    button(type="submit") Enregistrer